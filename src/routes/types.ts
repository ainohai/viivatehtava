export type Line = {
    minWidth: number,
    maxWidth: number,
    wantedLines: number,
    minDistance: number,
    maxDistance: number,
    minHeight: number,
    maxHeight: number,
    angles: number[],
    id: string,
    colors: string[]
}