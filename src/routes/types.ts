export type Line = {
    minWidth: number,
    maxWidth: number,
    wantedLines: number,
    minHeight: number,
    maxHeight: number,
    id: string, //Would be better to have universal angle system. 
    colors: string[]
}